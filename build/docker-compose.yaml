version: '3.8'

services:
  vue-app:
    build:
      context: ../..
      dockerfile: live-api/build/Dockerfile-vue
    volumes:
      - ../../live-www:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5173 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  go-app:
    build:
      context: ../..
      dockerfile: live-api/build/Dockerfile-go
    volumes:
      - ../../live-api:/app
    networks:
      - app-network
    depends_on:
      - vue-app
    ports:
      - "3000:3000"

  nginx:
    build:
      context: ../..
      dockerfile: live-api/build/Dockerfile-nginx
    volumes:
      - ../../live-api/build/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
      - "443:443"
    networks:
      - app-network
    depends_on:
      - vue-app
      - go-app

networks:
  app-network:
    driver: bridge